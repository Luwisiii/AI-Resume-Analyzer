# Copy to frontend/.env. Vite only exposes variables prefixed with VITE_.
# Leave empty: the dev server proxies /api to Django, keeping the session cookie
# same-origin. Set it only if the API is served from another origin on the same site.
VITE_API_URL=
